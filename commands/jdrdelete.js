[module.exports = {
  name: 'jdrdelete',
  aliases: ['jdrremove', 'jdrsuppr'],
  description: 'Supprime une campagne de JDR',
  //cooldown: 3,
  guildOnly: true,
  args: false,
  usage: '',
  execute(message, args, client) {
  },
}
]
//sql.run(`UPDATE scores SET points = ${row.points + 1}, level = ${row.level} WHERE userId = ${message.author.id}`);
